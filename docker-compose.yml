services:
  app:
    build: .
    image: face_analyze
    ports:
      - 8501:8501
    environment:
      PYTHONPYCACHEPREFIX: /tmp

  tests:
    build: .
    image: face_analyze
    command: pytest -s -v
    environment:
      PYTHONPYCACHEPREFIX: /tmp
    profiles: ["tests"]
